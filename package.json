{
  "name": "in-view",
  "version": "1.0.0",
  "description": "Get notified when a DOM element enters or exits the viewport.",
  "author": "Cam Wiegert <cam@camwiegert.com>",
  "license": "MIT",
  "scripts": {
    "start": "rollup -cw",
    "build": "rollup -c && npm run -s build:min",
    "build:min": "uglifyjs dist/in-view.umd.js > dist/in-view.min.js"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "rollup": "^0.50.0",
    "rollup-plugin-babel": "^3.0.2",
    "uglify-js": "^3.1.3"
  }
}
